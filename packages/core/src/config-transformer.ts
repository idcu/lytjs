// src/config-transformer.ts
// @lytjs/core - 配置转换器（复用独立包 @lytjs/config，保持向后兼容）

/**
 * 配置转换器
 *
 * 由独立包 @lytjs/config 提供，此处仅做兼容性转出。
 */
export { ConfigTransformer, transformConfig, mergeConfig } from '@lytjs/config';