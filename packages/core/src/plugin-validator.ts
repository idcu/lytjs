// src/plugin-validator.ts
// @lytjs/core - 插件验证器（复用独立包 @lytjs/plugin，保持向后兼容）

/**
 * 插件验证器
 *
 * 由独立包 @lytjs/plugin 提供，此处仅做兼容性转出。
 */
export { PluginValidator } from '@lytjs/plugin';
export type { ValidationReport, ValidationIssue } from '@lytjs/plugin';