/**
 * Lyt.js 模板编译器 — 补丁标记 (Patch Flags) 子路径入口
 *
 * import { getPatchFlag, hasPatchFlag, describePatchFlag } from '@lytjs/compiler/patch-flags'
 */

export { CompilerPatchFlags, getPatchFlag, hasPatchFlag, describePatchFlag } from './patch-flags';
