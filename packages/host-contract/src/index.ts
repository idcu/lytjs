// @lytjs/host-contract
// 统一渲染宿主接口定义（跨平台抽象契约）

export type {
  // 核心接口
  RendererHost,
  // 几何信息
  HostRect,
  // 样式信息
  HostStyleDeclaration,
  TransitionDurationInfo,
  // 事件类型
  HostEvent,
  HostEventHandler,
  HostEventOptions,
} from './types';
