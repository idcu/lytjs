import { defineConfig } from "tsup";

export default defineConfig({
  entry: ["src/index.ts"],
  format: ["esm", "cjs"],
  dts: true,
  sourcemap: true,
  clean: true,
  minify: false,
  target: "es2020",
  outDir: "dist",
  splitting: false,
  treeshake: true,
  // 外部依赖：所有 @lytjs/* 包都标记为外部依赖
  external: [/@lytjs\//],
});
